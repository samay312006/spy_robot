streams:
  picam:
    - "exec:ffmpeg -f v4l2 -input_format mjpeg -i /dev/video0 -c:v libx264 -preset ultrafast -b:v 1000k -f h264 -"

webrtc:
  candidates:
    - stun:stun.l.google.com:19302

api:
  listen: ":1984"
